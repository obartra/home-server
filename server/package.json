{
  "name": "server",
  "version": "1.0.0",
  "description": "Server",
  "license": "UNLICENSED",
  "dependencies": {
    "@types/node": "^18.13.0",
    "@types/qr-image": "^3.2.5",
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "qr-image": "^3.2.0",
    "simple-json-db": "^2.0.0",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "start": "yarn && rm -rf dist && tsc && node dist/index.js",
    "serve": "yarn && rm -rf dist && tsc && node dist/index.js --prod"
  }
}
